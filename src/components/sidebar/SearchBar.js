import React, { Component } from 'react'
import { Select } from 'semantic-ui-react'

export default class SearchBar extends Component {
  render() {
    return (
      // <Select placeholder='Select a State' options={countryOptions} />
      <div>Search Bar Here</div>
    )
  }
}
